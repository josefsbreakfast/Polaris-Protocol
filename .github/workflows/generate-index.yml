name: 🌀 Generate Markdown Index

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 0 * * *'

permissions:
  contents: write

jobs:
  test-run:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This proves the workflow runs."
